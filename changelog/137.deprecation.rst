``basic_wcs`` on `~irispy.sji.SJICube` and `~irispy.spectrograph.SpectrogramCube` has been renamed to ``fits_wcs`` to make clear that it is the plain FITS WCS as opposed to the gWCS on ``.wcs``. The old name still works but emits a `DeprecationWarning` and will be removed in a future release.
